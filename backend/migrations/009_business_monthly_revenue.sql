-- ============================================
-- 009: 업체 자진 신고 월 매출 컬럼
-- --------------------------------------------
-- ROI 계산에서 하드코딩된 가상 매출(1천만원) 대신 업체가 직접 입력한 값을
-- 사용하기 위한 컬럼. 미입력 시 ROI 는 revenueBasis='assumed' 로 추정임을 밝힌다.
-- ============================================
ALTER TABLE businesses ADD COLUMN IF NOT EXISTS monthly_revenue INT;

# Environment variables (NEVER commit secrets)
.env
.env.local
.env.production
.env.staging

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs
logs/
*.log

# Uploads (user-generated content)
uploads/

# Deployment
.vercel/

# Claude Code (local machine settings)
.claude/settings.local.json

# Windows: accidental redirects to the NUL device create a literal `nul` file
nul

# 제미나이 원본 (장당 5MB · 2048px). 가공본은 app/assets/characters/ 에 있다.
캐릭터이미지/

# 골든 테스트 실패 시 Flutter 가 떨어뜨리는 비교 이미지 (기대/실제/차분)
app/test/golden/failures/
